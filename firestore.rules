rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Public read for player autocomplete search (Top Order).
    match /players/{playerId} {
      allow read: if true;
      allow write: if false;
    }
  }
}
